{{-- Komponen flash sale (root div-nya sudah jadi section sendiri; saat tak ada
     flash sale aktif, ia kosong tanpa background — tidak ada band cream). --}}
@livewire('components.flash-saletimer')
